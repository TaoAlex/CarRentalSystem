All back-end code are found in the CarRentalSystem project, using the Entity Framework
All unit tests are found in CarRentalSystemUnitTests project, using the MSTest framework
The UI is found in the ConsoleUI project.

To start the UI, set the ConsoleUI project as the StartUp project and run.

The database that has been tested is local.

Contact: Alex_t93@live.se

